import { all } from 'redux-saga/effects';

// Project import

// ==============================|| ROOT SAGA - COMBINE ALL SAGAS  ||============================== //

function* rootSaga() {
    yield all([]);
}

export default rootSaga;
