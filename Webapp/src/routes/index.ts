import { useRoutes } from 'react-router-dom';
// project import

// ==============================|| ROUTES-APP  ||============================== //

const RoutesApp = () => useRoutes([]);

export default RoutesApp;
