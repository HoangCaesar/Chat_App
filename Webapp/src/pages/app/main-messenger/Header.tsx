import { Box, Stack } from '@mui/material';

// ==============================|| MESSENGER: HEADER ||============================== //

const Header = () => {
    return (
        <Box
            sx={{
                height: 100,
                width: '100%',
                backgroundColor: '#fff',
            }}
        >
            Header
        </Box>
    );
};

export default Header;
